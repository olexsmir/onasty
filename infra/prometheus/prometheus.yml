global:
  scrape_interval: 15s
  scrape_timeout: 10s
  evaluation_interval: 15s
scrape_configs:
  - job_name: core
    metrics_path: /metrics
    scheme: http
    follow_redirects: true
    honor_timestamps: true
    static_configs:
    - targets:
      - 127.0.0.1:3001
