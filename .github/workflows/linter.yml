name: linter

on:
  push:
  pull_request:

jobs:
  golang:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Golangci Lint
        uses: golangci/golangci-lint-action@v3
        with:
          version: latest
          args: ./...
