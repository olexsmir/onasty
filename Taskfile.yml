version: '3'

dotenv:
  - ".env.dev"

includes:
  migrate: ./migrations/Taskfile.yml

tasks:
  build:
    - go build -o .bin/onasty ./cmd/apiserver/

  run:
    - task: build
    - .bin/onasty

  docker:up:
    - docker compose up -d
